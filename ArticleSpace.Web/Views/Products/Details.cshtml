@model ProductViewModel

@{
    ViewData["Title"] = Model.Title;
}

<div class="row g-4 justify-content-center">
    <div class="col-md-6">
        @if (!string.IsNullOrEmpty(Model.ImageUrl))
        {
            <img src="@Model.ImageUrl" alt="@Model.Title" class="product-image-large" />
        }
    </div>
    <div class="col-md-6">
        <h1>@Model.Title</h1>
        <p class="badge badge-category">@Model.Category</p>
        <h3 class="text-primary">@Model.Price.ToString("C")</h3>

        <div class="mb-3">
            <strong>Rating:</strong> <span class="text-muted">★ @Model.Rating.Rate (@Model.Rating.Count)</span>
        </div>

        <article>
            <p>@Model.Description</p>
        </article>

        <div class="mt-4">
            <a asp-controller="Products" asp-action="Index" class="btn btn-secondary">Back</a>
        </div>
    </div>
</div>